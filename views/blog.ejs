<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog | Ylsoo</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .blog-header { text-align: center; padding: 100px 20px 40px; }
    .blog-header h1 { font-size: 42px; font-weight: 700; color: var(--dark-text); }
    .blog-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 24px;
      max-width: 1200px;
      margin: 0 auto;
      padding: 40px 20px 100px;
    }
    .blog-card {
      background: var(--white);
      border: 1px solid var(--border-color);
      border-radius: 14px;
      overflow: hidden;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .blog-card:hover { transform: translateY(-4px); box-shadow: 0 4px 14px rgba(0,0,0,0.08); }
    .blog-card img { width: 100%; height: 200px; object-fit: cover; }
    .blog-card-content { padding: 24px; }
    .blog-card-content h3 { margin-bottom: 10px; }
    .blog-card-content p { color: var(--mid-gray); margin-bottom: 16px; }
    .blog-card-content a { color: var(--primary-blue); font-weight: 600; }
  </style>
</head>
<body>
  <%- include('partials/navbar', { user }); %>

  <section class="blog-header">
    <h1>Ylsoo Blog</h1>
    <p>Insights, updates, and stories from the Ylsoo team.</p>
  </section>

  <div class="blog-grid">
    <% if (posts.length > 0) { %>
      <% posts.forEach(post => { %>
        <div class="blog-card">
          <% if (post.cover_image) { %>
            <img src="<%= post.cover_image %>" alt="<%= post.title %>">
          <% } %>
          <div class="blog-card-content">
            <h3><%= post.title %></h3>
            <p><%= post.excerpt %></p>
            <a href="/blog/<%= post.slug %>">Read More →</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p style="text-align:center; color:var(--mid-gray); font-size:16px;">No blog posts available yet.</p>
    <% } %>
  </div>

  <%- include('partials/footer'); %>
</body>
</html>
