<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Account - Ylsoo</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: #f5f7fb;
      font-family: 'Segoe UI', sans-serif;
      color: #222;
    }

    .dashboard-header {
      background: linear-gradient(135deg, #0078D4 0%, #00A4EF 100%);
      color: white;
      padding: 3rem 2rem;
      border-radius: 0 0 24px 24px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    .dashboard-header h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .dashboard-header p {
      font-size: 1rem;
      opacity: 0.9;
    }

    .profile-card {
      display: flex;
      align-items: center;
      background: white;
      border-radius: 16px;
      padding: 1.5rem 2rem;
      margin-top: -3rem;
      box-shadow: 0 4px 20px rgba(0,0,0,0.05);
    }

    .avatar-circle {
      width: 72px;
      height: 72px;
      background: #0078D4;
      color: white;
      font-size: 2rem;
      font-weight: bold;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 1.5rem;
    }

    .profile-info h2 {
      margin: 0;
      font-size: 1.5rem;
    }

    .profile-info p {
      color: #555;
      margin: 0.3rem 0;
    }

    .account-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
      gap: 1.2rem;
      margin-top: 2rem;
    }

    .account-card {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .account-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 4px 14px rgba(0,0,0,0.1);
    }

    .account-card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.4rem;
    }

    .account-card p {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 1rem;
    }

    .account-card a {
      color: #0078D4;
      font-weight: 600;
      text-decoration: none;
    }

    .account-card a:hover {
      text-decoration: underline;
    }

    .notifications-bar {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-top: 1.5rem;
    }

    .notifications-bar .bell {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      background: white;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      position: relative;
    }

    .notifications-bar .bell svg {
      width: 20px;
      height: 20px;
      stroke: #0078D4;
    }

    .notifications-bar .badge {
      position: absolute;
      top: 4px;
      right: 6px;
      background: red;
      color: white;
      border-radius: 50%;
      font-size: 0.7rem;
      width: 16px;
      height: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .section {
      margin-top: 3rem;
    }

    .section h2 {
      font-size: 1.4rem;
      margin-bottom: 1rem;
    }

    .recent-activity {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .activity-item {
      display: flex;
      align-items: center;
      border-bottom: 1px solid #eee;
      padding: 0.7rem 0;
    }

    .activity-item:last-child {
      border-bottom: none;
    }

    .activity-item svg {
      width: 20px;
      height: 20px;
      margin-right: 0.8rem;
      stroke: #0078D4;
    }

    .activity-item p {
      margin: 0;
      font-size: 0.9rem;
    }

    .btn-primary {
      background: #0078D4;
      border: none;
      color: white;
      padding: 0.7rem 1.2rem;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
    }

    .btn-primary:hover {
      background: #005ea6;
    }
  </style>
</head>

<body>
  <!-- Header -->
  <div class="dashboard-header">
    <h1>Welcome back, <%= user.name %> 👋</h1>
    <p>Manage your Ylsoo account, preferences, and activity</p>
  </div>

  <!-- Profile Summary -->
  <div class="container">
    <div class="profile-card">
      <div class="avatar-circle"><%= user.name.charAt(0).toUpperCase() %></div>
      <div class="profile-info">
        <h2><%= user.name %></h2>
        <p><%= user.email %></p>
        <div class="notifications-bar">
          <a href="/notifications" class="bell">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V4a2 2 0 10-4 0v1.341C8.67 6.165 8 7.388 8 8.75V14l-1.405 1.405A2.032 2.032 0 016 17h5m4 0v1a3 3 0 11-6 0v-1m6 0H9" />
            </svg>
            <span class="badge" id="notif-count" style="display:none;">0</span>
          </a>
          <button class="btn-primary" onclick="logout()">Sign out</button>
        </div>
      </div>
    </div>

    <!-- Account Sections -->
    <div class="account-grid">
      <div class="account-card">
        <h3>Profile & Info</h3>
        <p>Update your personal details and contact info.</p>
        <a href="/account/profile">Manage →</a>
      </div>

      <div class="account-card">
        <h3>Security</h3>
        <p>Passwords, devices, and sign-in protection.</p>
        <a href="/account/security">Manage →</a>
      </div>

      <div class="account-card">
        <h3>Notifications</h3>
        <p>Choose how Ylsoo keeps you informed.</p>
        <a href="/account/notifications">Customize →</a>
      </div>

      <div class="account-card">
        <h3>Devices</h3>
        <p>View your logged-in devices and active sessions.</p>
        <a href="/devices">See all →</a>
      </div>

      <div class="account-card">
        <h3>Billing & Payments</h3>
        <p>Manage payment methods and invoices.</p>
        <a href="/account/billing">Open →</a>
      </div>

      <div class="account-card">
        <h3>Privacy & Data</h3>
        <p>Control your data access and sharing preferences.</p>
        <a href="/account/data">Adjust →</a>
      </div>
    </div>

    <!-- Activity -->
    <div class="section">
      <h2>Recent Activity</h2>
      <div class="recent-activity" id="activity-feed">
        <div class="activity-item">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path d="M12 20h9" /><path d="M16 16V4a1 1 0 0 0-1-1H5a1 1 0 0 0-1 1v16l4-4h8a1 1 0 0 0 1-1z" />
          </svg>
          <p>Signed in via Chrome on Windows • Just now</p>
        </div>
        <div class="activity-item">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
            <circle cx="12" cy="7" r="4" />
          </svg>
          <p>Account created • Today</p>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script>
    async function logout() {
      const res = await fetch('/api/logout', { method: 'POST' });
      const data = await res.json();
      if (data.success) window.location.href = data.redirect;
    }

    // Real-time notification count (optional)
    const supabase = window.supabase.createClient(
      '<%= process.env.SUPABASE_URL || "https://qjnwvmzrwxdtvapsuzgc.supabase.co" %>',
      '<%= process.env.SUPABASE_ANON_KEY || "" %>'
    );

    supabase.channel('notif-count')
      .on('postgres_changes', { event: '*', schema: 'public', table: 'notifications' }, payload => {
        if (payload.new && payload.new.user_id === '<%= user.id %>' && !payload.new.is_read) {
          const badge = document.getElementById('notif-count');
          let count = parseInt(badge.textContent || 0) + 1;
          badge.textContent = count;
          badge.style.display = 'flex';
        }
      })
      .subscribe();
  </script>
</body>
</html>
