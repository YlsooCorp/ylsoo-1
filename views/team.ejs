      <!DOCTYPE html>
      <html lang="en">
      <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>The Ylsoo Team</title>
        <link rel="stylesheet" href="/css/style.css">
        <style>
          body {
            background: #f9fafb;
            color: #111827;
            font-family: 'Inter', system-ui, sans-serif;
            margin: 0;
          }

          .hero {
            text-align: center;
            padding: 5rem 1.5rem 3rem;
          }

          .hero h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
          }

          .hero p {
            color: #6b7280;
            font-size: 1.1rem;
          }

          .team-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 2rem;
            max-width: 1100px;
            margin: 0 auto;
            padding: 2rem;
          }

          .member-card {
            background: #ffffff;
            border-radius: 14px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.05);
            text-align: center;
            padding: 2rem 1.2rem;
            transition: all 0.25s ease;
          }

          .member-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 6px 22px rgba(0,0,0,0.1);
          }

          .member-photo {
            width: 96px;
            height: 96px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 1rem;
            box-shadow: 0 0 0 4px #e5e7eb;
          }

          .member-name {
            font-size: 1.2rem;
            font-weight: 600;
          }

          .member-role {
            font-size: 0.95rem;
            color: #6b7280;
            margin-bottom: 0.8rem;
          }

          .member-bio {
            font-size: 0.9rem;
            color: #374151;
            line-height: 1.5;
            min-height: 60px;
          }

          .socials {
            margin-top: 0.8rem;
          }

          .socials a {
            color: #111827;
            margin: 0 0.4rem;
            text-decoration: none;
            transition: color 0.2s ease;
          }

          .socials a:hover {
            color: #2563eb;
          }
        </style>
      </head>
      <body>
        <%- include('partials/navbar', { user }) %>

        <section class="hero">
          <h1>Meet the Ylsoo Team</h1>
          <p>The humans behind the vision — innovators, engineers, and ethical AI dreamers.</p>
        </section>

        <section class="team-grid">
          <% if (team.length > 0) { %>
            <% team.forEach(member => { %>
              <div class="member-card">
                <img src="<%= member.photo_url || '/img/default-avatar.png' %>" alt="<%= member.name %>" class="member-photo">
                <div class="member-name"><%= member.name %></div>
                <div class="member-role"><%= member.role %></div>
                <p class="member-bio"><%= member.bio || '—' %></p>
                <div class="socials">
                  <% if (member.linkedin_url) { %>
                    <a href="<%= member.linkedin_url %>" target="_blank" aria-label="LinkedIn">
                      <i class="fa-brands fa-linkedin"></i>
                    </a>
                  <% } %>
                  <% if (member.twitter_url) { %>
                    <a href="<%= member.twitter_url %>" target="_blank" aria-label="Twitter">
                      <i class="fa-brands fa-x-twitter"></i>
                    </a>
                  <% } %>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <p style="text-align:center;color:#6b7280;">No team members found yet.</p>
          <% } %>
        </section>

        <%- include('partials/footer') %>
        <script src="https://kit.fontawesome.com/5d2df64e9a.js" crossorigin="anonymous"></script>
      </body>
      </html>
